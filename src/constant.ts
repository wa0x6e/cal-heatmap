export enum FillStrategy {
  RESET_ALL_ON_UPDATE,
  RESET_SINGLE_ON_UPDATE,
  APPEND_ON_UPDATE,
}

export enum ScrollDirection {
  SCROLL_NONE,
  SCROLL_BACKWARD,
  SCROLL_FORWARD,
}

export enum Position {
  TOP,
  RIGHT,
  BOTTOM,
  LEFT,
}
