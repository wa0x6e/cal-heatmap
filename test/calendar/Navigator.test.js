// import Navigator from '../../src/calendar/Navigator';

describe('loadNextDomain()', () => {
  xit('returns false if max domain is reached', () => {});

  xit('returns false if range is 0', () => {});
});
describe('loadPreviousDomain()', () => {
  xit('returns false if min domain is reached', () => {});

  xit('returns false if range is 0', () => {});
});
describe('jumpTo()', () => {
  xit('', () => {});
});
describe('loadNewDomains()', () => {
  xit('', () => {});
});
